#!/usr/bin/env bash
set -e
rm -rf "$DEST"

# This script exists as backwards compatibility for CI
(
	DEST="$***REMOVED***DEST***REMOVED***-daemon"
	ABS_DEST="$***REMOVED***ABS_DEST***REMOVED***-daemon"
	. hack/make/binary-daemon
)
